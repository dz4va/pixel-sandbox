using System.Collections;
using System.Collections.Generic;
using UnityEngine;

enum Direction {
    Down, DownRight, DownLeft,
    Up, UpRight, UpLeft,
    Left, Right
}

public class SandboxAutomata : MonoBehaviour
{
    [SerializeField]
    Texture2D sandboxTexture;
    [SerializeField]
    float simulationSpeed;
    [SerializeField]
    

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
